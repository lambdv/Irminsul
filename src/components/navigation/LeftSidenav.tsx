import RightSidenavCSS from '@/components/navigation/rightsidenav.module.css'

export default function LeftSidenav(props) {
  return (
      // <nav className={`${RightSidenavCSS.leftsidenav} ${props.className}`} >
      //   {props.children}
      // </nav>
    <></>
    )
}