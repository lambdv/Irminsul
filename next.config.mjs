/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        // dynamicIO: true,
    },
    // images: {
    //     domains: ['www.irminsul.moe'],
    // }
};

export default nextConfig;