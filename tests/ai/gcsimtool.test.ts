import '@testing-library/jest-dom'

describe('DPSTool', () => {
    it('works', async () => {
        // const result = await queryGCSIMDatabase({
        //     characters: ['Ganyu'],
        // })
        // console.log(result)
    })
})
